import { TriggerNode, ActionNode, ConditionNode, DelayNode } from '../components/nodes';

export const nodeTypes = {
  trigger: TriggerNode,
  action: ActionNode,
  condition: ConditionNode,
  delay: DelayNode,
};